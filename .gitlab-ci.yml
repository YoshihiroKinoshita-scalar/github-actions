image: docker:stable

build:
  stage: build
  tags:
    - docker
  script:
    - apk add --no-cache docker-compose
    - export DEVELDIR=`pwd`
    - docker-compose up -d
    - docker-compose exec -T server bash
    - docker-compose exec sh buil.sh



